<Window x:Class="WPF_MD.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Список сотрудников"
        Height="560"
        Width="980"
        MinHeight="560"
        MinWidth="980">
    <DockPanel Margin="16">
        <Border DockPanel.Dock="Top"
                Padding="12"
                Margin="0,0,0,12"
                Background="#FFF5F5F5"
                CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,12,0">
                    <TextBlock FontWeight="SemiBold" Text="Поиск" />
                    <TextBox Margin="0,6,0,0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,12,0">
                    <TextBlock FontWeight="SemiBold" Text="Фильтр по отделу" />
                    <ComboBox Margin="0,6,0,0"
                              ItemsSource="{Binding Departments}"
                              SelectedItem="{Binding SelectedDepartment}" />
                </StackPanel>

                <StackPanel Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Bottom">
                    <Button Width="110" Margin="0,0,8,0" Command="{Binding LoadCommand}" Content="Загрузить" />
                    <Button Width="110" Margin="0,0,8,0" Command="{Binding SaveCommand}" Content="Сохранить" />
                    <Button Width="130" Command="{Binding SaveAsCommand}" Content="Сохранить как" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel DockPanel.Dock="Bottom"
                    Margin="0,12,0,0"
                    Orientation="Horizontal">
            <Button Width="110" Margin="0,0,8,0" Command="{Binding AddCommand}" Content="Добавить" />
            <Button Width="110" Margin="0,0,8,0" Command="{Binding EditCommand}" Content="Редактировать" />
            <Button Width="110" Margin="0,0,8,0" Command="{Binding DeleteCommand}" Content="Удалить" />
            <TextBlock VerticalAlignment="Center"
                       Foreground="DimGray"
                       Text="{Binding CurrentFilePath}" />
        </StackPanel>

        <DataGrid ItemsSource="{Binding EmployeesView}"
                  SelectedItem="{Binding SelectedEmployee}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Width="2.2*" Binding="{Binding FullName}" Header="ФИО" />
                <DataGridTextColumn Width="1.6*" Binding="{Binding Position}" Header="Должность" />
                <DataGridTextColumn Width="1.4*" Binding="{Binding Department}" Header="Отдел" />
                <DataGridTextColumn Width="0.8*" Binding="{Binding Age}" Header="Возраст" />
                <DataGridTextColumn Width="1.8*" Binding="{Binding Email}" Header="Email" />
                <DataGridTextColumn Width="1.3*" Binding="{Binding Phone}" Header="Телефон" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
